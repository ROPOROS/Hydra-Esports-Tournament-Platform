{% extends 'base.html.twig' %}

{% block title %}Edit Joueur{% endblock %}

{% block body %}
    <div style="margin: 6rem 6rem; display: flex; flex-direction: column; justify-content: center; align-items: center; gap: 40px;" >
        <h1>Edit Joueur</h1>

        {{ include('joueur/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('joueur_showf') }}">back to list</a>

        {{ include('joueur/_delete_form.html.twig') }}

    </div>


{% endblock %}
